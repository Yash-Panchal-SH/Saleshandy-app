# Shared, non-secret defaults. Committed.
# Per-environment values live in .env.development / .env.staging / .env.production.
# Secrets belong in .env.*.local (gitignored).
VITE_PWA_ENABLED=false

VITE_API_BASE_URL=/api
